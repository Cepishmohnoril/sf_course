{% extends 'base.html.twig' %}

{% block title %}Hello DefaultController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/sf_course/theory/src/Controller/DefaultController.php'|file_link(0) }}">src/Controller/DefaultController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/sf_course/theory/templates/default/index.html.twig'|file_link(0) }}">templates/default/index.html.twig</a></code></li>
    </ul>
</div>

<ul>

{% for user in users %}
    <li>{{user.name}}{% if can_send[loop.index0] %} - Can send mail.{% endif %}</li>
{% endfor %}

</ul>

{% for message in app.flashes('notice') %}
    <div class="flash-notice">
        {{ message }}
    </div>
{% endfor %}


{% set foo = '-5' %}

<p>The abs of {{foo}} is {{foo|abs}}</p>

<p>{{"And don't yell at me."|upper}}</p>

{{ include('includes/monke.html.twig', { 'users': users }) }}

{{ path('tpl') }} </br>
{{ url('tpl') }} </br>
{{ asset('image/logo.png') }} </br>
{{ absolute_url(asset('image/logo.png')) }} </br>

{{ "Escape \nthe \nMonke"|e }} </br>

{% endblock %}

{% block javascript %}
    {{ parent() }}
    <script>
        console.log('doot');
    </script>
{% endblock %}